{
  "name": "omarchy-kaj",
  "version": "0.1.0",
  "private": true,
  "description": "Docker containers in the Omarchy bar: live status, stats, logs, and controls, grouped by Compose project.",
  "scripts": {
    "test": "node --test",
    "seed": "dev/seed",
    "clean": "dev/clean"
  },
  "license": "MIT"
}
